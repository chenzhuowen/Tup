<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${groupId}.${rootArtifactId}.mapper.TimeStampMapper">


    <select id="queryCurrentTimeStamp" resultType="java.lang.String">
        select CURRENT_TIMESTAMP;
    </select>
    <select id="queryUtcTimeStamp" resultType="java.lang.String">
        select UTC_TIMESTAMP;
    </select>
</mapper>